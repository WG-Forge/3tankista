#pragma once

enum GameState
{
    NOT_INITIALIZED = 0,
    INITIALIZED,
    RESTARTED,
    STARTED,
    RUNNING,
    PROCESSING,
    GAMEOVER,
    TERMINATED,
    MAX_GAMESTATES
}; // enum GameState
